# Normalize line endings: store text as LF in the repo, check out native.
* text=auto eol=lf

# Treat data/binary formats as binary (no EOL conversion, no diff noise).
*.dcm  binary
*.fif  binary
*.edf  binary
*.nii  binary
*.gz   binary
*.mgz  binary
*.h5   binary
*.png  binary
